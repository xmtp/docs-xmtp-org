import { InteractiveAgentDemo } from '../../../components/InteractiveAgentDemo';

# Try an agent

Chat with Agent Frostbeard, an agent powered by Claude AI. The messages you exchange with the agent are end-to-end encrypted using the IETF-standard MLS (Messaging Layer Security).

<InteractiveAgentDemo />

By building with XMTP, you automatically get the security and privacy guarantees of a messaging standard trusted by Google, AWS, and Wire, to name a few other adopters.

## Build your own

Ready to build an agent like this? Follow the step-by-step tutorial to connect your own AI agent to XMTP from scaffolding to deployment.

<a href="/agents/build-agents/connect-to-xmtp" onClick={() => window.plausible?.('Demo: Build Your Own Clicked')}>Connect your agent to XMTP</a>
